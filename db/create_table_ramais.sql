CREATE TABLE ramais (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50),
    Host VARCHAR(50),
    Dyn VARCHAR(1),
    Nat VARCHAR(1),
    ACL VARCHAR(1),
    Port INT,
    Status VARCHAR(50)
);

